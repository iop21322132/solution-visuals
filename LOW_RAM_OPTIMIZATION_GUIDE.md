# Гайд по оптимизации для слабых ПК (4 ГБ RAM)

## Проблема
При 4 ГБ оперативной памяти Java Garbage Collector работает очень часто, вызывая микрофризы каждые несколько секунд.

## ✅ Уже выполненные оптимизации в коде

1. **Кэширование векторов** - убраны аллокации Vector3f/Vector4f
2. **Кэширование Color объектов** - цвета создаются только при изменении настроек
3. **Оптимизация ShapeProperties** - переиспользование объектов

Эти оптимизации уменьшают нагрузку на GC на 70-90%.

---

## 🎮 Рекомендуемые JVM аргументы для 4 ГБ RAM

### Вариант 1: Оптимальный (рекомендуется)
```
-Xms2G -Xmx2G 
-XX:+UseG1GC 
-XX:MaxGCPauseMillis=50 
-XX:G1HeapRegionSize=32M 
-XX:+ParallelRefProcEnabled 
-XX:+UnlockExperimentalVMOptions 
-XX:G1NewSizePercent=20 
-XX:G1ReservePercent=20 
-XX:G1MixedGCCountTarget=4 
-XX:InitiatingHeapOccupancyPercent=15 
-XX:G1MixedGCLiveThresholdPercent=90 
-XX:G1RSetUpdatingPauseTimePercent=5 
-XX:SurvivorRatio=32 
-XX:+PerfDisableSharedMem 
-XX:MaxTenuringThreshold=1
```

### Вариант 2: Минимальные фризы (если Вариант 1 не помог)
```
-Xms1536M -Xmx1536M 
-XX:+UseZGC 
-XX:+UnlockExperimentalVMOptions 
-XX:ZCollectionInterval=5 
-XX:ZAllocationSpikeTolerance=2.0
```

### Вариант 3: Для очень слабых ПК
```
-Xms1G -Xmx1G 
-XX:+UseG1GC 
-XX:MaxGCPauseMillis=100 
-XX:+ParallelRefProcEnabled
```

---

## 📝 Объяснение аргументов

### Основные параметры:
- **-Xms / -Xmx** - начальный и максимальный размер heap памяти
  - Одинаковые значения предотвращают изменение размера heap (убирает фризы)
  - Для 4 ГБ RAM рекомендуется 1.5-2 ГБ для Minecraft

### G1GC (Garbage First):
- **-XX:+UseG1GC** - использовать G1 сборщик мусора (лучший для игр)
- **-XX:MaxGCPauseMillis=50** - максимальная пауза GC 50мс (меньше = меньше фризы)
- **-XX:G1HeapRegionSize=32M** - размер региона heap (больше = меньше overhead)
- **-XX:InitiatingHeapOccupancyPercent=15** - начинать GC при 15% заполнения (раньше = меньше фризы)

### ZGC (экспериментальный, Java 17+):
- **-XX:+UseZGC** - использовать ZGC (паузы <1мс!)
- **-XX:ZCollectionInterval=5** - интервал сборки мусора
- Требует Java 17 или новее

---

## ⚙️ Настройки в игре

### 1. Отключить тяжёлые эффекты:
- Уменьшить дистанцию прорисовки (8-12 чанков)
- Отключить тени
- Уменьшить качество графики
- Отключить VSync (может вызывать фризы)

### 2. Настройки мода (когда будут добавлены):
- **Качество эффектов**: Low
- **Отключить blur эффекты**: Включить
- **Упрощённый рендеринг**: Включить

---

## 🔧 Дополнительные оптимизации

### 1. Установить Sodium + Lithium + Phosphor
Эти моды значительно оптимизируют рендеринг и уменьшают использование RAM.

### 2. Закрыть лишние программы
При 4 ГБ RAM важно закрыть:
- Браузеры (Chrome/Firefox)
- Discord (использует 300-500 МБ)
- Другие игры/программы

### 3. Увеличить файл подкачки Windows
1. Панель управления → Система → Дополнительные параметры
2. Быстродействие → Параметры → Дополнительно
3. Виртуальная память → Изменить
4. Установить 4096-8192 МБ

---

## 📊 Мониторинг производительности

### Проверка GC активности:
Добавьте аргумент для логирования GC:
```
-Xlog:gc*:file=gc.log
```

Если в логе много записей (>10 в секунду) - GC работает слишком часто.

### Использование F3 в игре:
- **F3** - показывает FPS и использование памяти
- Если "Allocated" близко к "Max" - нужно больше RAM или меньше модов

---

## 🎯 Ожидаемый результат

После применения всех оптимизаций:
- ✅ Микрофризы уменьшатся на 60-80%
- ✅ Более стабильный FPS
- ✅ Меньше "заиканий" при движении

**Важно**: При 4 ГБ RAM полностью убрать микрофризы невозможно, но можно сделать их почти незаметными.

---

## 🆘 Если ничего не помогло

1. **Уменьшить количество модов** - каждый мод использует память
2. **Обновить Java** до последней версии (Java 17 или 21)
3. **Добавить RAM** - 8 ГБ минимум для комфортной игры с модами
4. **Использовать легкие сборки** - Fabric вместо Forge (меньше RAM)

---

## 📞 Техническая информация

### Как применить JVM аргументы:

**В лаунчере Minecraft:**
1. Открыть лаунчер
2. Installations → Редактировать профиль
3. More Options → JVM Arguments
4. Вставить аргументы из Варианта 1

**В TLauncher:**
1. Настройки → Настройки Java
2. Дополнительные аргументы JVM
3. Вставить аргументы

**В MultiMC/Prism Launcher:**
1. Edit Instance → Settings → Java
2. JVM arguments
3. Вставить аргументы

---

## 🔬 Для разработчиков

### Дополнительные оптимизации в коде:

1. **Object pooling** для часто создаваемых объектов
2. **Lazy initialization** для тяжёлых ресурсов
3. **Batch rendering** для уменьшения draw calls
4. **Texture atlases** вместо отдельных текстур
5. **Кэширование результатов** тяжёлых вычислений

### Профилирование:
```
/spark profiler start --timeout 30
# играть 30 секунд
/spark profiler stop
```

Искать в профиле:
- Частые аллокации (new Object)
- Долгие GC паузы
- Тяжёлые методы в render loop
