# Контекстное меню с анимацией 🎨

## Что это?

Красивое анимированное меню, которое появляется при нажатии **ПКМ** в чате или любом GUI.

## ⚠️ ВАЖНО: Как включить

Контекстное меню реализовано как **модуль**. Чтобы оно работало:

1. Откройте меню модов (по умолчанию **Insert** или через ClickGUI)
2. Найдите модуль **"Context Menu"** в категории **Visuals**
3. **Включите** модуль
4. Теперь можно использовать меню!

## Как использовать?

1. **Включите модуль** "Context Menu" (см. выше)
2. Откройте **чат** (T) или любой **GUI** (E, инвентарь)
3. Нажмите **ПКМ** (правую кнопку мыши) в любом месте
4. Меню появится с плавной анимацией!
5. **Выбрать пункт**: Наведите курсор и нажмите **ЛКМ**
6. **Закрыть меню**: 
   - Нажмите **ESC**
   - Кликните вне меню
   - Выберите любой пункт

## Эффекты ✨

### Плавное появление
- Меню появляется с эффектом масштабирования
- Плавное изменение прозрачности
- Длительность: ~400ms

### Эффект "колбасы" 🌊
- После появления меню слегка покачивается
- Затухающие колебания (4 раза)
- Создает ощущение "живого" интерфейса
- Длительность: 600ms

### Плавное закрытие
- Обратная анимация при закрытии
- Меню исчезает плавно

### Blur эффект 🌫️
- Размытые края
- Полупрозрачный фон
- Закругленные углы
- Мягкая обводка

### Интерактивность
- Подсветка при наведении на пункт
- Плавная анимация подсветки
- Изменение цвета текста

## Пункты меню (по умолчанию)

- 📋 **Копировать** - копирует выбранное
- 📝 **Вставить** - вставляет из буфера
- ✂ **Вырезать** - вырезает выбранное
- 🔄 **Обновить** - обновляет данные
- ⚙ **Настройки** - открывает настройки

## Технические детали

### Анимации
- **Scale**: `EASE_OUT_ELASTIC` (bounce эффект)
- **Alpha**: `EASE_OUT_CUBIC` (плавное затухание)
- **Wobble**: `EASE_OUT_SINE` (покачивание)

### Параметры
- Ширина: 150px
- Высота пункта: 28px
- Радиус закругления: 10px
- Blur softness: 2.0

### Цвета
- Фон: цвет темы (альфа 160)
- Обводка: цвет темы (альфа 200)
- Текст: белый (альфа 200-255)
- Подсветка: белый (альфа 30)

## Как настроить?

### Изменить пункты меню

Откройте `ContextMenuModule.java` и найдите метод `createContextMenu()`:

```java
private ContextMenu createContextMenu() {
    ContextMenu menu = new ContextMenu();
    
    // Добавьте свои пункты
    menu.addItem("Мой пункт", () -> {
        // Ваш код здесь
    });
    
    return menu;
}
```

### Изменить размеры

Откройте `ContextMenu.java` и измените:

```java
private float width = 150f;      // Ширина меню
private float itemHeight = 28f;  // Высота пункта
private float padding = 6f;      // Отступы
```

### Изменить анимацию

В конструкторе `ContextMenu`:

```java
// Изменить тип и длительность
private final Animation scaleAnimation = 
    new Animation(Easing.EASE_OUT_BACK, 500);
```

Доступные типы:
- `EASE_OUT_ELASTIC` - сильный bounce (текущий)
- `EASE_OUT_BACK` - легкий overshoot
- `EASE_OUT_CUBIC` - плавное
- `EASE_OUT_QUART` - быстрое
- `LINEAR` - линейное

## Где работает?

✅ Чат  
✅ Инвентарь  
✅ Любые GUI  
✅ Кастомные экраны  

## Производительность

- Легковесное (минимум аллокаций)
- Анимации только когда видимо
- Оптимизированный рендеринг
- Кэширование цветов

## Совместимость

✅ Не конфликтует с другими модами  
✅ Работает с любыми темами  
✅ Совместимо со всеми миксинами  

## Идеи для улучшения

- [ ] Подменю (вложенные пункты)
- [ ] Иконки вместо эмодзи
- [ ] Горячие клавиши
- [ ] Разделители между группами
- [ ] Чекбоксы и радио-кнопки
- [ ] Динамические пункты по контексту
- [ ] Звуковые эффекты
- [ ] Настройка через GUI

---

**Приятного использования! 🎉**
